

# Algorithm for putVal()

hash(key);

if(table == null)
resize();

index = (n - 1) & hash;

if(bucket empty)
insert;

else{
if(same key)
update;

    else if(tree)
        tree insertion;

    else{
        linked list traversal;

        if(key found)
            update;

        else
            append node;

        if(length >= 8)
            treeify;
    }
}

size++;

if(size > threshold)
resize();

# Algorithm of getNode()

hash(key)

↓

bucket

↓

first node

↓

hash match?

↓

equals?

↓

return

↓

linked list?

↓

continue

↓

tree?

↓

tree search